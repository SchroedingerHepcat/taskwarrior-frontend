[package]
name = "server"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
taskwarrior_core = { path = "../core" }
taskwarrior_compat = { path = "../taskwarrior_compat" }
uuid = { version = "1.18.1", features = ["std"] }

[lib]
path = "src/lib.rs"
